<div class="box">
    <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center">
            <strong>{{ block.translate(app.request.locale).translatableFields.title|raw }}</strong>
        </h2>
        <hr>
        <h2 class="intro-text text-center">
            {{ block.translate(app.request.locale).translatableFields.description|raw }}
        </h2>
    </div>
    {% for product in products %}
        <div class="col-sm-4 text-center">
            <img class="img-responsive" src="{{ asset('bundles/app/images/cover.jpg') }}" alt="">
            <h3>{{ product.title }}</h3>
            <h5>
                {{ product.rawDescription|raw }}
            </h5>
        </div>
    {% endfor %}
</div>